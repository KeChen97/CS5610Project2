function Index() {
    const index = {};
    const tempDiv = document.querySelector("div.history");


    async function fetchTemplates () {
    }

index.fetchTemplates = fetchTemplates;
return index;
}

const index = Index();
index.fetchTemplates();
